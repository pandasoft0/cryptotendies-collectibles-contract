/*
const TendiesBox = artifacts.require("../contracts/TendiesBox.sol");

contract("TendiesBox", (accounts) => {
  let tendiesBox;

  before(async () => {
    tendiesBox = await TendiesBox.deployed();
  });

  describe('#constructor()', () => {
    it('should set the URI to the supplied value', async () => {
      assert.equal(await tendiesBox.uri(1), "https://metadata.tendies.dev/api/box/1");
    });
  });
});
*/